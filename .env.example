# Required for live classification. Without it, CivicLens falls back to a local heuristic.
OPENAI_API_KEY=sk-your-key-here

# Optional. Defaults to gpt-4o-mini (cheap, fast, vision-capable).
OPENAI_MODEL=gpt-4o-mini
